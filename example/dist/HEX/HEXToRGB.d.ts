/**
 * HEX to RGB
 *
 * @example
 * #rgb => rgb(r,g,b)
 * #rgba => rgb(r,g,b,a)
 * #rrggbb => rgb(r,g,b)
 * #rrggbbaa => rgba(r,g,b,a)
 *
 * @param h hex
 *
 * @returns RGB string
 */
declare function HEXToRGB(h: string): string;
export default HEXToRGB;
